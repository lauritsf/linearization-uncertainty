# @package _global_

defaults:
  - override /datamodule: qm9

datamodule:
  batch_size: 64
  num_workers: 2

trainer:
  max_steps: 100000
  val_check_interval: 8000
  accumulate_grad_batches: 8

sampling:
  num_samples: 512
  max_length: 500

model:
  model_name: llama-s
  attention_dropout: 0.5

# @package _global_

defaults:
  - override /datamodule: planar

datamodule:
  batch_size: 16
  num_workers: 2

trainer:
  max_steps: 200000
  val_check_interval: 8000
  accumulate_grad_batches: 8

model:
  model_name: llama2-s
  attention_dropout: 0.5
